# Email Config Map - Exact Line Numbers & Copy/Paste\n\n## 📍 File Location\n```\nserver/utils/email/mailer.js\n```\n\n---\n\n## 🗺️ Configuration Map\n\n```\nLINE 1-13:     Header comments explaining everything\nLINE 15-37:    ✅ PRODUCTION CONFIG (Active)\nLINE 39-44:    Dev section header\nLINE 46-78:    🧪 DEV OPTION A: Ethereal\nLINE 80-99:    🧪 DEV OPTION B: MailHog\nLINE 102-126:  🧪 DEV OPTION C: Local Gmail\nLINE 128-142:  Verification code (always active)\n```\n\n---\n\n## 📋 How to Switch Configurations\n\n### Switch FROM Production TO Ethereal\n\n**Step 1:** Find line 18\n```javascript\nexport const transporter = nodemailer.createTransport({  // ← LINE 18 - COMMENT THIS OUT\n```\nAdd `//` at the start to comment it out:\n```javascript\n// export const transporter = nodemailer.createTransport({\n```\n\n**Step 2:** Find line 50 (Ethereal section)\nUncomment the entire Ethereal block (lines 50-78)\n\nChange:\n```javascript\n(async () => {                           // ← UNCOMMENT THIS\n  const testAccount = await ...          // ← UNCOMMENT THIS\n```\n\nTo (uncommented):\n```javascript\n(async () => {\n  const testAccount = await ...\n```\n\n**Step 3:** Restart\n```bash\nctrl+c  # Stop current server\nnpm start\n```\n\n---\n\n### Switch FROM Ethereal TO Production\n\n**Step 1:** Comment out Ethereal (lines 50-78)\n\n**Step 2:** Uncomment Production (line 18)\nChange:\n```javascript\n// export const transporter = nodemailer.createTransport({\n```\nTo:\n```javascript\nexport const transporter = nodemailer.createTransport({\n```\n\n**Step 3:** Restart\n```bash\nctrl+c\nnpm start\n```\n\n---\n\n## 📑 Copy-Paste Blocks\n\n### PRODUCTION (Lines 18-37) - Copy entire block\n\n```javascript\nexport const transporter = nodemailer.createTransport({\n  service: \"gmail\",\n  auth: {\n    user: process.env.MAIL_USER,        // Your Gmail address\n    pass: process.env.MAIL_PASS,        // Gmail App Password (16 chars, NOT regular password)\n  },\n  // ✅ Production-grade timeout & connection settings\n  connectionTimeout: 10000,  // 10 seconds to establish connection\n  socketTimeout: 10000,      // 10 seconds for socket operations\n  pool: {\n    maxConnections: 3,       // Limit concurrent SMTP connections (prevents rate limits)\n    maxMessages: 100,        // Messages per connection before recycling\n    rateDelta: 1000,         // milliseconds between messages\n    rateLimit: 10,           // Messages per rateDelta (respects Gmail rate limits)\n  },\n  // ✅ TLS settings for Render compatibility\n  secure: true,              // Use TLS encryption\n  requireTLS: true,          // Require TLS negotiation\n});\n```\n\n### DEV: ETHEREAL (Lines 50-78) - Copy entire block\n\n```javascript\n// Uncomment this entire block for Ethereal testing:\n(async () => {\n  const testAccount = await nodemailer.createTestAccount();\n  \n  const devTransporter = nodemailer.createTransport({\n    host: testAccount.smtp.host,\n    port: testAccount.smtp.port,\n    secure: testAccount.smtp.secure,\n    auth: {\n      user: testAccount.user,\n      pass: testAccount.pass,\n    },\n    // Dev: Relaxed timeouts for debugging\n    connectionTimeout: 30000,  // 30 seconds (more lenient)\n    socketTimeout: 30000,\n    pool: {\n      maxConnections: 1,       // Single connection for dev\n      maxMessages: 50,\n    },\n  });\n  \n  devTransporter.verify((err) => {\n    if (err) console.error(\"❌ Ethereal error:\", err.message);\n    else console.log(\"✅ Ethereal ready - View emails: https://ethereal.email\");\n  });\n  \n  // Replace the export above with this:\n  export { devTransporter as transporter };\n})();\n```\n\n### DEV: MAILHOG (Lines 81-99) - Copy entire block\n\n```javascript\n// Uncomment this entire block for MailHog testing:\nexport const transporter = nodemailer.createTransport({\n  host: \"127.0.0.1\",\n  port: 1025,                // Default MailHog SMTP port\n  secure: false,             // No TLS needed for localhost\n  connectionTimeout: 30000,  // Dev: More lenient\n  socketTimeout: 30000,\n  pool: {\n    maxConnections: 2,\n    maxMessages: 50,\n  },\n});\n\ntransporter.verify((err) => {\n  if (err) {\n    console.error(\"❌ MailHog error:\", err.message);\n    console.error(\"⚠️  Make sure MailHog is running: http://localhost:1025\");\n  } else {\n    console.log(\"✅ MailHog SMTP ready\");\n    console.log(\"📧 View emails: http://localhost:1025\");\n  }\n});\n```\n\n### DEV: LOCAL GMAIL (Lines 102-126) - Copy entire block\n\n```javascript\n// Uncomment this entire block for local Gmail testing:\nexport const transporter = nodemailer.createTransport({\n  service: \"gmail\",\n  auth: {\n    user: process.env.MAIL_USER,\n    pass: process.env.MAIL_PASS,\n  },\n  // Dev: More lenient than production\n  connectionTimeout: 30000,  // 30 seconds (more time for debugging)\n  socketTimeout: 30000,\n  pool: {\n    maxConnections: 2,\n    maxMessages: 50,\n  },\n  secure: true,\n  requireTLS: true,\n});\n\ntransporter.verify((err) => {\n  if (err) {\n    console.error(\"❌ Gmail SMTP error:\", err.message);\n  } else {\n    console.log(\"✅ Gmail SMTP ready (LOCAL DEV MODE)\");\n  }\n});\n```\n\n---\n\n## 🎯 Key Takeaway\n\n**You only need to change `mailer.js` - nothing else!**\n\n✅ Password reset emails automatically use whatever transporter is exported\n✅ All retries and timeouts work automatically  \n✅ No changes needed to `passwordReset.js`, `sendWithRetry.js`, etc.\n✅ Just uncomment/comment the config you want\n\n---\n\n## 🚀 One-Command Switch\n\n### Switch to Ethereal\n```bash\n# 1. Open mailer.js\n# 2. Comment line 18: // export const transporter = ...\n# 3. Uncomment lines 50-78: (async () => { ... export { devTransporter ...}\n# 4. Save file\n# 5. npm start\n```\n\n### Switch to MailHog  \n```bash\n# 1. Open mailer.js\n# 2. Comment line 18: // export const transporter = ...\n# 3. Uncomment lines 81-99: export const transporter = ...\n# 4. Save file\n# 5. npm start\n```\n\n### Switch to Production\n```bash\n# 1. Open mailer.js  \n# 2. Uncomment line 18: export const transporter = ...\n# 3. Comment all dev sections\n# 4. Save file\n# 5. npm start\n```\n\n---\n\n## ✅ Verification Checklist\n\nAfter switching config:\n- [ ] File saved\n- [ ] Old config commented out\n- [ ] New config uncommented\n- [ ] Server restarted: `ctrl+c` then `npm start`\n- [ ] See `✅ SMTP is ready` in console\n- [ ] Test forgot password works\n\n**All done!** ✨\n