{
  "name": "inventory-management",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "server"
  ],
  "scripts": {
    "build": "npm ci && npm run build --workspace frontend",
    "start": "npm run start --workspace server",
    "postinstall": "npm run postinstall --workspace frontend || true"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
