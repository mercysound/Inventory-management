{
  "name": "inventory-management",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend",
"start": "npm run start --workspace=server"  
  }
}
