# ✅ Production Deployment Checklist - Email Timeout Fix\n\n## Pre-Deployment Verification (DO BEFORE PUSHING)\n\n### Environment Variables on Render\n- [ ] `MAIL_USER` is set to your Gmail address\n- [ ] `MAIL_PASS` is set to a Gmail **App Password** (16 characters, NOT regular password)\n- [ ] `FRONTEND_URL` is set to your actual frontend domain (e.g., `https://yourapp.render.com`)\n- [ ] All env vars were created in Render dashboard (not in .env file)\n\n### Get Gmail App Password\n1. Go to: https://myaccount.google.com/apppasswords\n2. Select \"Mail\" and \"Windows Computer\" (or your device)\n3. Copy the 16-character password\n4. Set `MAIL_PASS` to this value in Render\n\n### Local Testing (Optional but Recommended)\n```bash\n# Test backend email sending\ncd server\nnpm start\n# Should see: \"✅ SMTP is ready\"\n\n# Test frontend in another terminal\ncd frontend\nnpm run dev\n# Go to http://localhost:5173/forgot-password\n# Enter an email and verify email arrives or see helpful error message\n```\n\n## Deployment Steps\n\n### Step 1: Review Changes\n- [ ] Review `server/utils/email/mailer.js` - SMTP config updated\n- [ ] Review `server/utils/email/sendWithRetry.js` - NEW file created\n- [ ] Review `frontend/src/utils/api.js` - Timeout extended to 60s for emails\n- [ ] Review `frontend/src/pages/ForgotPassword.jsx` - Better error messages\n\n### Step 2: Push to Git\n```bash\ncd d:\\Inventory-management\ngit status  # Should show modified files\ngit add .\ngit commit -m \"Fix: Add SMTP timeout configuration and retry logic for password reset emails\"\ngit push origin main  # or your branch\n```\n\n### Step 3: Monitor Render Deployment\n1. Go to Render dashboard\n2. Select your service\n3. Go to \"Logs\" tab\n4. Wait for deploy to complete\n5. Look for green checkmark and \"✅ SMTP is ready\" message\n\n### Step 4: Test in Production\n```\n1. Go to https://yourapps.render.com/forgot-password\n2. Enter your test email (use a real one you can check)\n3. Click \"Send Reset Link\"\n4. Wait up to 60 seconds\n5. Check your email for password reset link\n6. Should arrive within 10-15 seconds if SMTP is healthy\n```\n\n### Step 5: Check Render Logs for Success\nShould see:\n```\n✅ SMTP is ready\n📧 Sending email (attempt 1/3)...\n✅ Email sent successfully: <message-id>\n```\n\nIf instead you see:\n```\n❌ SMTP error: Error message\n```\n\nThen check:\n- [ ] `MAIL_USER` is correct Gmail address\n- [ ] `MAIL_PASS` is correct 16-character app password\n- [ ] Gmail 2FA is enabled (required for app passwords)\n- [ ] App password was generated for \"Mail\"\n\n## Rollback Plan (If Something Goes Wrong)\n\n```bash\n# In case you need to revert\ngit log --oneline  # Find previous commit\ngit revert HEAD\ngit push\n# Render will auto-deploy the reverted version\n```\n\n## Expected Behavior After Fix\n\n### ✅ Good Signs\n- Password reset emails arrive within 60 seconds\n- If SMTP is slow, retry message shows in logs\n- User sees helpful message if service is down\n- Multiple emails sent successfully in a row\n\n### ⚠️ Warning Signs\n- Emails still timing out after 60 seconds\n- Error: `ETIMEDOUT` or `ECONNREFUSED` (repeated)\n- \"SMTP error\" on startup\n- Users reporting still getting 30-second timeouts\n\n### 🔧 Troubleshooting Warnings\n\n**Issue:** \"SMTP error: Invalid login\" on startup\n**Fix:** Check `MAIL_PASS` is a 16-char app password, not regular password\n\n**Issue:** Emails arrive after 30+ seconds\n**Fix:** Render might be cold-starting. Try again - should be faster.\n\n**Issue:** Emails never arrive\n**Fix:** Check `FRONTEND_URL` in env vars - reset link might have wrong domain\n\n## Success Criteria\n\nDeployment is successful when:\n- [ ] Render logs show \"✅ SMTP is ready\"\n- [ ] Password reset email arrives within 15 seconds\n- [ ] Subsequent emails also succeed\n- [ ] No timeout errors appear on frontend\n- [ ] Forgot password page shows helpful error messages (if any)\n\n## Support URLs\n\n- **Gmail App Passwords:** https://myaccount.google.com/apppasswords\n- **Gmail Security Settings:** https://myaccount.google.com/security\n- **Render Logs:** https://dashboard.render.com → Select Service → Logs\n- **Nodemailer SMTP Issues:** https://nodemailer.com/smtp/\n\n## Quick Reference: What Changed\n\n| Component | Change | Benefit |\n|-----------|--------|----------|\n| SMTP Config | Added timeouts + pooling | Prevents hanging connections |\n| Email Send | Added 3-retry logic | Handles transient failures |\n| Frontend Timeout | 10s → 60s (email routes) | Enough time for retries |\n| Error Messages | User-friendly messages | Users know what's happening |\n\n---\n\n**Questions?** Check `EMAIL_TIMEOUT_FIX_GUIDE.md` for detailed troubleshooting.\n