# Email Configuration - Quick Reference Card\n\n## 🎯 Answer: Do You Need to Change Anything for Password Emails?\n\n### **NO!** ✅\n\nPassword reset emails automatically work with ANY email configuration. Everything just works.\n\n---\n\n## 🚀 Current Setup (Production)\n\n**File:** `server/utils/email/mailer.js` (line 18)\n\n```javascript\nexport const transporter = nodemailer.createTransport({\n  service: \"gmail\",\n  auth: { user: process.env.MAIL_USER, pass: process.env.MAIL_PASS },\n  // Timeouts: 10 seconds (production-grade)\n});\n```\n\n**Status:** ✅ Active and working  \n**Timeouts:** 10 seconds (strict)  \n**Best for:** Production on Render\n\n---\n\n## 🧪 Dev Testing Options\n\n### Option A: Ethereal (Easiest)\n**File:** `server/utils/email/mailer.js` (line 50)\n\n```javascript\n// Uncomment lines 50-78 in mailer.js\n// Timeouts: 30 seconds (more lenient)\n// No setup needed - automatic\n```\n\n**How to switch:**\n1. Comment out production `export const transporter` (line 18-37)\n2. Uncomment Ethereal block (lines 50-78)\n3. Change `export { devTransporter as transporter }` to `export const transporter`\n4. `npm start`\n5. See console URL to view test emails\n\n### Option B: MailHog (Best UI)\n**File:** `server/utils/email/mailer.js` (line 81)\n\n```javascript\n// Uncomment lines 81-99 in mailer.js\n// Timeouts: 30 seconds (more lenient)\n// Need to run MailHog first\n```\n\n**How to switch:**\n1. Download: https://github.com/mailhog/MailHog\n2. Run: `./mailhog`\n3. Comment out production transporter (lines 18-37)\n4. Uncomment MailHog block (lines 81-99)\n5. `npm start`\n6. View emails: http://localhost:8025\n\n### Option C: Local Gmail (Same as Prod)\n**File:** `server/utils/email/mailer.js` (line 102)\n\n```javascript\n// Uncomment lines 102-126 in mailer.js\n// Timeouts: 30 seconds (more lenient for debugging)\n// Uses real Gmail - emails actually sent\n```\n\n**How to switch:**\n1. Get Gmail App Password from: https://myaccount.google.com/apppasswords\n2. Add to `.env`: `MAIL_PASS=xxxx xxxx xxxx xxxx`\n3. Comment out production transporter (lines 18-37)\n4. Uncomment Gmail dev block (lines 102-126)\n5. `npm start`\n6. Check email inbox for real emails\n\n---\n\n## 📝 Configuration Timeout Differences\n\n| Config | Timeout | Reason |\n|--------|---------|--------|\n| **Production** | 10 sec | Strict, optimized |\n| **Ethereal** | 30 sec | More lenient for debugging |\n| **MailHog** | 30 sec | More lenient for debugging |\n| **Local Gmail** | 30 sec | More lenient for debugging |\n\n**Why different?** Dev configs give extra time for debugging and testing.\n\n---\n\n## ✅ Password Reset Emails Work Automatically\n\nAll these files use the `transporter` from `mailer.js`:\n- `passwordReset.js` - Uses `sendWithRetry()` ✅\n- `customerProcessing.js` - Uses `sendWithRetry()` ✅\n- `customerOrderDelivered.js` - Uses `sendWithRetry()` ✅\n- `adminOrderPlaced.js` - Uses `sendWithRetry()` ✅\n\n**No changes needed** - just change `mailer.js` and all emails use it! 🎉\n\n---\n\n## 🔍 How to Verify Configuration is Working\n\n```bash\n# 1. Start backend\ncd server && npm start\n\n# Expected output:\n# ✅ SMTP is ready - Email service verified and working\n```\n\n**If you see this:** Configuration is working ✅\n\n**If you see error:** Check your config in `mailer.js`\n\n---\n\n## 🧪 Quick Test Workflow\n\n```bash\n# Terminal 1: Start backend\ncd server && npm start\n# Should see: ✅ SMTP is ready\n\n# Terminal 2: Start frontend  \ncd frontend && npm run dev\n\n# Open browser: http://localhost:5173/forgot-password\n# Enter email: test@example.com\n# Click: Send Reset Link\n# Check: See email in Ethereal/MailHog/Gmail inbox\n\n# Password reset email works! ✅\n```\n\n---\n\n## 📋 Common Issues & Fixes\n\n| Issue | Fix |\n|-------|-----|\n| \"Connection timeout\" | Make sure MailHog running or check internet |\n| \"Connection refused\" | MailHog not running - start it first |\n| \"Invalid login\" | Use Gmail App Password, not regular password |\n| Email not arriving | Give it 3-30 seconds (retries may be happening) |\n\n---\n\n## 🎯 Bottom Line\n\n**Question:** Do you need to change password email config?\n\n**Answer:** NO! ✅\n\n**Why?** Because all email functions automatically use whatever `transporter` is exported from `mailer.js`, including the retry logic and timeouts.\n\n**What to do:** Just uncomment your preferred dev config or use production config.\n\n**Result:** Password reset emails work perfectly every time! 🚀\n